<% content_for :title, "New todo" %>

<div class="mb-8">
  <h1 class="text-3xl font-bold text-gray-900">New Todo</h1>
  <p class="mt-2 text-sm text-gray-600">Create a new todo item.</p>
</div>

<%= render "form", todo: @todo %>

<div class="mt-6">
  <%= link_to "Back to todos", todos_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
</div>
